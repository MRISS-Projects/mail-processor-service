runtime: java11

instance_class: F1

inbound_services:
  - mail
  - warmup

service: "@service.name@"

env_variables:
  ALLOWED_ACCOUNT: "@allowed.account@"
  ALLOWED_SENDER_LIST: "@allowed.sender.list@"
